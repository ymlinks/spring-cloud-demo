version '1.0.0'
bootJar {
    launchScript()
}

dependencies {
    compile project(":boot-common")
    compile("org.springframework.boot:spring-boot-starter-data-elasticsearch") {
        exclude module: "transport"
    }
    compile("org.elasticsearch.client:x-pack-transport:5.6.1")
}
