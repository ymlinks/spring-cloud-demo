server:
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain
    min-response-size: 1
  servlet:
    context-path: /cloud

spring:
  application:
    name: boot-demo
  cloud:
    config:
      uri: http://localhost:8888
      username: test
      password: test
eureka:
  client:
    serviceUrl:
      defaultZone: http://120.27.229.180:8761/eureka/
  instance:
    hostname: localhost
    metadata-map:
      zone: ${spring.profiles.active:dev}
grpc:
  port: 6565